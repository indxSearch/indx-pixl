import React from "react";
type IconProps = {
    /** Overrides every fill. Omit to keep the icon's own level fills (var(--lvN)). */
    color?: string;
    size?: number | string;
};
declare const Virtual_reality_vr_headset: React.FC<IconProps>;
export default Virtual_reality_vr_headset;
