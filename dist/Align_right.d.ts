import React from "react";
type IconProps = {
    /** Overrides every fill. Omit to keep the icon's own level fills (var(--lvN)). */
    color?: string;
    size?: number | string;
};
declare const Align_right: React.FC<IconProps>;
export default Align_right;
