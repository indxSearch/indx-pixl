import React from "react";
type IconProps = {
    /** Overrides every fill. Omit to keep the icon's own level fills (var(--lvN)). */
    color?: string;
    size?: number | string;
};
declare const Four_leaf_clover: React.FC<IconProps>;
export default Four_leaf_clover;
